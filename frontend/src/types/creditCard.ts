// src/types/creditCard.ts

export interface CreditCard {
    id: string;
    name: string;
    cardNumber: string;
    expirationDate: string; // Ou Date, se preferir trabalhar com objetos Date
  }
  